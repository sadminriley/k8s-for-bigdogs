apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-tls
  namespace: argocd
spec:
  secretName: argocd-server-tls
  dnsNames:
    - argocd.dev
  issuerRef:
    name: local-ca-issuer
    kind: ClusterIssuer
